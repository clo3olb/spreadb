// The function generated here must be copied to spreadsheets' internal script

/**
 * Creates UUID.
 *
 * @return random UUID.
 * @customfunction
 */
function UUID() {
  return Utilities.getUuid();
}
